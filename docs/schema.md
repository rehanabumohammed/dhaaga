# Dhaaga — database schema

**Generated by `scripts/gen_schema_doc.py`. Do not edit by hand.**

Every table carries the standard spine (ADR-0004) — `id`, `business_id`, `created_at`, `created_by`, `updated_at`, `updated_by`, `deleted_at`, `row_version` — which is omitted from the column lists below. Transactional tables additionally carry `branch_id`.

**90 tables** across six clusters.

## 2.1 Tenancy, identity, audit

### `business`

The tenant root. Exactly one row in V1; the schema is multi-tenant from day one (AP-7).

| column | type | |
|---|---|---|
| `legal_name` | text | not null |
| `trade_name` | text |  |
| `gstin` | text |  |
| `pan` | text |  |
| `address_line1` | text |  |
| `address_line2` | text |  |
| `city` | text |  |
| `state_code` | text |  |
| `postal_code` | text |  |
| `country_code` | text | not null · default `'IN'::text` |
| `default_locale` | text | not null · default `'en-IN'::text` |
| `currency_code` | text | not null · default `'INR'::text` |
| `financial_year_start_month` | smallint | not null · default `4` |
| `status` | text | not null · default `'active'::text` |

Constraints:

- `business_fy_month_valid` — CHECK (((financial_year_start_month >= 1) AND (financial_year_start_month <= 12)))
- `business_status_valid` — CHECK ((status = ANY (ARRAY['active'::text, 'suspended'::text, 'closed'::text])))

### `branch`

An outlet. Owns cash, stock, capacity, document series and its own profit and loss.

| column | type | |
|---|---|---|
| `code` | text | not null |
| `name` | text | not null |
| `gstin` | text |  |
| `state_code` | text |  |
| `address_line1` | text |  |
| `address_line2` | text |  |
| `city` | text |  |
| `postal_code` | text |  |
| `phone_e164` | text |  |
| `timezone` | text | not null · default `'Asia/Kolkata'::text` |
| `opened_on` | date |  |
| `is_active` | boolean | not null · default `true` |

References: `business`

### `app_user`

A person the business knows: staff, whether or not they log in. Authentication is a separate fact (auth_user_id); branch access is granted separately through user_branch_role.

| column | type | |
|---|---|---|
| `full_name` | text | not null |
| `display_name` | text |  |
| `phone_e164` | text |  |
| `email` | text |  |
| `employee_code` | text |  |
| `locale` | text |  |
| `status` | text | not null · default `'active'::text` |
| `last_seen_at` | timestamp with time zone |  |
| `auth_user_id` | uuid |  |

References: `business`

Constraints:

- `app_user_status_valid` — CHECK ((status = ANY (ARRAY['invited'::text, 'active'::text, 'suspended'::text, 'archived'::text])))

### `permission`

The permission vocabulary, seeded per business. is_books_surface marks the accounting boundary of AP-2.

| column | type | |
|---|---|---|
| `code` | text | not null |
| `domain` | text | not null |
| `description` | text |  |
| `is_books_surface` | boolean | not null · default `false` |

References: `business`

### `role`

A named permission set. Seeded per business; the owner may adjust which permissions each role holds.

| column | type | |
|---|---|---|
| `code` | text | not null |
| `name` | text | not null |
| `description` | text |  |
| `is_system` | boolean | not null · default `false` |
| `sort_order` | integer | not null · default `100` |

References: `business`

### `role_permission`

Which permissions a role holds. Editable by the owner: roles are seeded, their contents are configuration (AP-1).

| column | type | |
|---|---|---|
| `role_id` | uuid | not null |
| `permission_id` | uuid | not null |

References: `business`, `permission`, `role`, `permission`, `role`

### `user_branch_role`

One row per (person, branch, role). A manager covering two outlets has two rows and one login.

| column | type | |
|---|---|---|
| `user_id` | uuid | not null |
| `branch_id` | uuid | not null |
| `role_id` | uuid | not null |
| `granted_at` | timestamp with time zone | not null · default `now()` |
| `granted_by` | uuid |  |
| `revoked_at` | timestamp with time zone |  |
| `revoked_by` | uuid |  |
| `revoke_reason` | text |  |

References: `branch`, `business`, `branch`, `role`, `app_user`, `role`, `app_user`

### `device`

A registered client device. Every offline action is attributed to one (§5.4).

| column | type | |
|---|---|---|
| `label` | text |  |
| `platform` | text |  |
| `model` | text |  |
| `app_version` | text |  |
| `last_user_id` | uuid |  |
| `home_branch_id` | uuid |  |
| `first_seen_at` | timestamp with time zone | not null · default `now()` |
| `last_seen_at` | timestamp with time zone |  |
| `last_sync_at` | timestamp with time zone |  |
| `offline_minutes_total` | bigint | not null · default `0` |
| `is_active` | boolean | not null · default `true` |

References: `business`, `branch`, `app_user`

Constraints:

- `device_platform_valid` — CHECK ((platform = ANY (ARRAY['android'::text, 'ios'::text, 'web'::text, 'windows'::text, 'other'::text])))

### `number_series`

One counter per branch, document type and financial year (BR-14).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `doc_type` | text | not null |
| `financial_year` | text | not null |
| `prefix` | text | not null · default `''::text` |
| `suffix` | text | not null · default `''::text` |
| `padding` | smallint | not null · default `5` |
| `next_value` | integer | not null · default `1` |
| `is_offline_leasable` | boolean | not null · default `false` |

References: `branch`, `business`

Constraints:

- `number_series_doc_type_valid` — CHECK ((doc_type = ANY (ARRAY['order_token'::text, 'job_card'::text, 'tax_invoice'::text, 'bill_of_supply'::text, 'credit_note'::text, 'delivery_note'::text, 'estimate'::text, 'payment_receipt'::text, 'purchase_bill'::text, 'stock_transfer'::text, 'wage_payout'::text])))
- `number_series_next_positive` — CHECK ((next_value >= 1))
- `number_series_padding_valid` — CHECK (((padding >= 1) AND (padding <= 12)))

### `number_lease`

A block of numbers handed to a device so an offline token prints its final number (§5.3).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `series_id` | uuid | not null |
| `device_id` | uuid | not null |
| `range_start` | integer | not null |
| `range_end` | integer | not null |
| `next_value` | integer | not null |
| `issued_at` | timestamp with time zone | not null · default `now()` |
| `expires_at` | timestamp with time zone | not null |
| `status` | text | not null · default `'active'::text` |
| `closed_at` | timestamp with time zone |  |

References: `branch`, `business`, `device`, `number_series`

Constraints:

- `number_lease_next_in_range` — CHECK (((next_value >= range_start) AND (next_value <= (range_end + 1))))
- `number_lease_no_overlap` — EXCLUDE USING gist (series_id WITH =, int4range(range_start, range_end, '[]'::text) WITH &&) WHERE ((deleted_at IS NULL))
- `number_lease_range_valid` — CHECK ((range_end >= range_start))
- `number_lease_status_valid` — CHECK ((status = ANY (ARRAY['active'::text, 'exhausted'::text, 'expired'::text, 'returned'::text])))

### `number_void`

Every number issued but never used, with a reason. This is what makes each gap in a register explainable (BR-14).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `series_id` | uuid | not null |
| `lease_id` | uuid |  |
| `value` | integer | not null |
| `reason_code` | text | not null |
| `reason_text` | text |  |
| `voided_at` | timestamp with time zone | not null · default `now()` |
| `voided_by` | uuid |  |

References: `branch`, `business`, `number_lease`, `number_series`

### `audit_event`

Append-only record of who changed what, when, from where (BR-13). No update or delete policy exists for any application role, and the privileges are revoked besides.

| column | type | |
|---|---|---|
| `branch_id` | uuid |  |
| `entity_type` | text | not null |
| `entity_id` | uuid |  |
| `action` | text | not null |
| `before_data` | jsonb |  |
| `after_data` | jsonb |  |
| `changed_fields` | text[] |  |
| `reason_code` | text |  |
| `reason_text` | text |  |
| `actor_user_id` | uuid |  |
| `device_id` | uuid |  |
| `source` | text | not null · default `'app'::text` |
| `occurred_at` | timestamp with time zone | not null · default `now()` |
| `recorded_at` | timestamp with time zone | not null · default `now()` |

References: `app_user`, `branch`, `business`, `device`

Constraints:

- `audit_event_action_valid` — CHECK ((action = ANY (ARRAY['insert'::text, 'update'::text, 'delete'::text, 'login'::text, 'logout'::text, 'grant'::text, 'revoke'::text, 'export'::text, 'other'::text])))
- `audit_event_source_valid` — CHECK ((source = ANY (ARRAY['app'::text, 'web'::text, 'system'::text, 'migration'::text, 'console'::text])))

## 2.2 Configuration, localisation, tax

### `locale`

Languages this business operates in. V1 activates English; Hindi is architecturally present from P0.

| column | type | |
|---|---|---|
| `code` | text | not null |
| `name` | text | not null |
| `native_name` | text |  |
| `direction` | text | not null · default `'ltr'::text` |
| `is_active` | boolean | not null · default `true` |
| `is_default` | boolean | not null · default `false` |
| `sort_order` | integer | not null · default `100` |

References: `business`

Constraints:

- `locale_direction_valid` — CHECK ((direction = ANY (ARRAY['ltr'::text, 'rtl'::text])))

### `translation`

Translations for tenant-owned data labels. App chrome is translated in the client message catalogues; this covers what the owner creates.

| column | type | |
|---|---|---|
| `entity_type` | text | not null |
| `entity_id` | uuid | not null |
| `field` | text | not null |
| `locale_code` | text | not null |
| `text_value` | text | not null |

References: `business`

### `config_setting`

The registry of every owner-controllable rule (AP-1, BR-21). A literal threshold or rate in application code is a defect.

| column | type | |
|---|---|---|
| `key` | text | not null |
| `category` | text | not null |
| `scope` | text | not null · default `'business'::text` |
| `value_type` | text | not null |
| `default_value` | jsonb | not null |
| `current_value` | jsonb |  |
| `allowed_values` | jsonb |  |
| `required_permission` | text | not null |
| `is_effective_dated` | boolean | not null · default `false` |
| `is_ca_validated_scope` | boolean | not null · default `false` |
| `description` | text |  |

References: `business`

Constraints:

- `config_setting_scope_valid` — CHECK ((scope = ANY (ARRAY['business'::text, 'branch'::text])))
- `config_setting_type_valid` — CHECK ((value_type = ANY (ARRAY['boolean'::text, 'integer'::text, 'decimal'::text, 'text'::text, 'date'::text, 'json'::text, 'enum'::text])))

### `config_version`

Every value a setting has ever held, with the window it applied in. This is what lets a past document be reproduced under the rule in force then (BR-15).

| column | type | |
|---|---|---|
| `setting_id` | uuid | not null |
| `value` | jsonb | not null |
| `effective_from` | timestamp with time zone | not null · default `now()` |
| `effective_to` | timestamp with time zone |  |
| `changed_by` | uuid |  |
| `changed_at` | timestamp with time zone | not null · default `now()` |
| `reason_code` | text |  |
| `reason_text` | text |  |

References: `business`, `app_user`, `config_setting`

Constraints:

- `config_version_no_overlap` — EXCLUDE USING gist (setting_id WITH =, tstzrange(effective_from, effective_to) WITH &&) WHERE ((deleted_at IS NULL))
- `config_version_range_valid` — CHECK (((effective_to IS NULL) OR (effective_to > effective_from)))

### `config_branch_override`

A branch-level value for a setting whose scope allows it. Absent means the business value applies.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `setting_id` | uuid | not null |
| `value` | jsonb | not null |
| `effective_from` | timestamp with time zone | not null · default `now()` |
| `effective_to` | timestamp with time zone |  |

References: `branch`, `business`, `config_setting`

Constraints:

- `config_branch_override_no_overlap` — EXCLUDE USING gist (setting_id WITH =, branch_id WITH =, tstzrange(effective_from, effective_to) WITH &&) WHERE ((deleted_at IS NULL))
- `config_branch_override_range_valid` — CHECK (((effective_to IS NULL) OR (effective_to > effective_from)))

### `reason_code`

The controlled lists behind every "why did you do that" prompt. The owner edits them; the domains they attach to are fixed.

| column | type | |
|---|---|---|
| `domain` | text | not null |
| `code` | text | not null |
| `label` | text | not null |
| `requires_text` | boolean | not null · default `false` |
| `is_active` | boolean | not null · default `true` |
| `sort_order` | integer | not null · default `100` |

References: `business`

Constraints:

- `reason_code_domain_valid` — CHECK ((domain = ANY (ARRAY['discount'::text, 'price_override'::text, 'order_cancellation'::text, 'date_override'::text, 'stock_adjustment'::text, 'stock_wastage'::text, 'payment_reversal'::text, 'refund'::text, 'measurement_change'::text, 'alteration'::text, 'wage_adjustment'::text, 'permission_change'::text, 'number_void'::text, 'material_writeoff'::text, 'other'::text])))

### `tax_profile`

The tax identity in force for a business or a branch over a period. Per-branch GSTIN is supported from day one (decision 2).

| column | type | |
|---|---|---|
| `branch_id` | uuid |  |
| `registration_type` | text | not null |
| `gstin` | text |  |
| `legal_name` | text |  |
| `place_of_supply` | text |  |
| `effective_from` | date | not null |
| `effective_to` | date |  |
| `is_active` | boolean | not null · default `true` |

References: `branch`, `business`

Constraints:

- `tax_profile_range_valid` — CHECK (((effective_to IS NULL) OR (effective_to > effective_from)))
- `tax_profile_registration_valid` — CHECK ((registration_type = ANY (ARRAY['regular'::text, 'composition'::text, 'unregistered'::text])))

### `tax_code`

A tax classification with its HSN or SAC code. Stitching is a service; fabric is goods; the rates attached to each are configuration.

| column | type | |
|---|---|---|
| `code` | text | not null |
| `kind` | text | not null |
| `hsn_sac` | text |  |
| `description` | text |  |
| `is_active` | boolean | not null · default `true` |

References: `business`

Constraints:

- `tax_code_kind_valid` — CHECK ((kind = ANY (ARRAY['service'::text, 'goods'::text])))

### `tax_rate`

A rate and the window it applies in. Reprinting a two-year-old invoice uses the row that was in force then, never today's (BR-15).

| column | type | |
|---|---|---|
| `tax_code_id` | uuid | not null |
| `total_percent` | app.rate_percent | not null |
| `effective_from` | date | not null |
| `effective_to` | date |  |
| `note` | text |  |

References: `business`, `tax_code`

Constraints:

- `tax_rate_no_overlap` — EXCLUDE USING gist (tax_code_id WITH =, daterange(effective_from, effective_to) WITH &&) WHERE ((deleted_at IS NULL))
- `tax_rate_percent_sane` — CHECK ((((total_percent)::numeric >= (0)::numeric) AND ((total_percent)::numeric <= (100)::numeric)))
- `tax_rate_range_valid` — CHECK (((effective_to IS NULL) OR (effective_to > effective_from)))

### `tax_rate_component`

The split a rate breaks into on an invoice. Intra-state work splits CGST and SGST; inter-state uses IGST.

| column | type | |
|---|---|---|
| `tax_rate_id` | uuid | not null |
| `component` | text | not null |
| `percent` | app.rate_percent | not null |

References: `business`, `tax_rate`

Constraints:

- `tax_rate_component_percent_sane` — CHECK ((((percent)::numeric >= (0)::numeric) AND ((percent)::numeric <= (100)::numeric)))
- `tax_rate_component_valid` — CHECK ((component = ANY (ARRAY['CGST'::text, 'SGST'::text, 'IGST'::text, 'UTGST'::text, 'CESS'::text])))

### `price_list`

A set of prices with a validity window. branch_id null means it applies business-wide; a branch row overrides it.

| column | type | |
|---|---|---|
| `branch_id` | uuid |  |
| `code` | text | not null |
| `name` | text | not null |
| `currency_code` | text | not null · default `'INR'::text` |
| `pricing_mode` | text | not null · default `'exclusive'::text` |
| `is_default` | boolean | not null · default `false` |
| `effective_from` | date | not null |
| `effective_to` | date |  |

References: `branch`, `business`

Constraints:

- `price_list_pricing_mode_valid` — CHECK ((pricing_mode = ANY (ARRAY['inclusive'::text, 'exclusive'::text])))
- `price_list_range_valid` — CHECK (((effective_to IS NULL) OR (effective_to > effective_from)))

### `notification_template`

What a customer message says, per event, channel and language. Editable by the owner without a release (AP-1).

| column | type | |
|---|---|---|
| `event_key` | text | not null |
| `channel` | text | not null |
| `locale_code` | text | not null |
| `subject` | text |  |
| `body` | text | not null |
| `is_active` | boolean | not null · default `true` |

References: `business`

Constraints:

- `notification_template_channel_valid` — CHECK ((channel = ANY (ARRAY['whatsapp'::text, 'sms'::text, 'email'::text, 'in_app'::text])))

### `validation_signoff`

A qualified CA's approval of the tax and accounting configuration, hashed against what was approved. Until a current row exists, the system runs unvalidated and tax documents are watermarked as drafts (BR-22).

| column | type | |
|---|---|---|
| `domain` | text | not null |
| `validated_by_name` | text | not null |
| `firm` | text |  |
| `reference` | text |  |
| `validated_on` | date | not null |
| `config_hash` | text | not null |
| `notes` | text |  |
| `is_current` | boolean | not null · default `true` |
| `superseded_at` | timestamp with time zone |  |
| `superseded_reason` | text |  |

References: `business`

Constraints:

- `validation_signoff_domain_valid` — CHECK ((domain = ANY (ARRAY['tax'::text, 'accounting'::text, 'payroll'::text])))

## 2.3 Customers, households, measurements

### `household`

A family or group served together. A lookup and a grouping - never a second ledger account. Members hold their own measurements, history and balances.

| column | type | |
|---|---|---|
| `name` | text | not null |
| `address_line1` | text |  |
| `address_line2` | text |  |
| `city` | text |  |
| `postal_code` | text |  |
| `notes` | text |  |

References: `business`

### `customer`

A person. Identity is the uuid; the phone is an attribute (§2.3). Business-scoped, so one record serves every branch.

| column | type | |
|---|---|---|
| `customer_code` | text |  |
| `display_name` | text | not null |
| `name_normalized` | text |  |
| `given_name` | text |  |
| `family_name` | text |  |
| `gender` | text |  |
| `date_of_birth` | date |  |
| `first_seen_branch_id` | uuid |  |
| `preferred_branch_id` | uuid |  |
| `preferred_locale` | text |  |
| `notes` | text |  |
| `status` | text | not null · default `'active'::text` |
| `merged_into_id` | uuid |  |

References: `business`, `branch`, `customer`, `branch`

Constraints:

- `customer_gender_valid` — CHECK ((gender = ANY (ARRAY['male'::text, 'female'::text, 'other'::text, 'unspecified'::text])))
- `customer_merge_state_coherent` — CHECK ((((status = 'merged'::text) AND (merged_into_id IS NOT NULL)) OR ((status <> 'merged'::text) AND (merged_into_id IS NULL))))
- `customer_not_merged_into_self` — CHECK (((merged_into_id IS NULL) OR (merged_into_id <> id)))
- `customer_status_valid` — CHECK ((status = ANY (ARRAY['active'::text, 'merged'::text, 'archived'::text])))

### `household_member`

Membership of a household, with a relation label. A join table from day one so shared households are a dropped index later, not a restructure.

| column | type | |
|---|---|---|
| `household_id` | uuid | not null |
| `customer_id` | uuid | not null |
| `relation` | text |  |
| `is_primary_contact` | boolean | not null · default `false` |

References: `business`, `customer`, `household`

Constraints:

- `household_member_relation_valid` — CHECK ((relation = ANY (ARRAY['head'::text, 'spouse'::text, 'son'::text, 'daughter'::text, 'father'::text, 'mother'::text, 'sibling'::text, 'other'::text])))

### `customer_contact`

A way to reach someone. Never unique: two people may share a number, one person may have six, and a person may have none (§2.3).

| column | type | |
|---|---|---|
| `customer_id` | uuid |  |
| `household_id` | uuid |  |
| `kind` | text | not null · default `'mobile'::text` |
| `value_raw` | text | not null |
| `value_e164` | text |  |
| `label` | text |  |
| `is_primary` | boolean | not null · default `false` |
| `is_verified` | boolean | not null · default `false` |
| `verified_at` | timestamp with time zone |  |

References: `business`, `customer`, `household`

Constraints:

- `customer_contact_kind_valid` — CHECK ((kind = ANY (ARRAY['mobile'::text, 'whatsapp'::text, 'landline'::text, 'email'::text, 'other'::text])))
- `customer_contact_owner_exactly_one` — CHECK ((((customer_id IS NOT NULL) AND (household_id IS NULL)) OR ((customer_id IS NULL) AND (household_id IS NOT NULL))))

### `duplicate_candidate`

A suggested duplicate awaiting human review. Nothing merges automatically: wrongly merging two family members who share a number is far worse than a duplicate (§2.3).

| column | type | |
|---|---|---|
| `customer_a_id` | uuid | not null |
| `customer_b_id` | uuid | not null |
| `score` | numeric(4,3) | not null |
| `reasons` | jsonb | not null · default `'{}'::jsonb` |
| `detected_by` | text | not null · default `'nightly'::text` |
| `status` | text | not null · default `'open'::text` |
| `reviewed_by` | uuid |  |
| `reviewed_at` | timestamp with time zone |  |
| `dismissed_reason` | text |  |

References: `business`, `customer`, `customer`, `app_user`

Constraints:

- `duplicate_candidate_detected_by_valid` — CHECK ((detected_by = ANY (ARRAY['live'::text, 'nightly'::text, 'staff'::text])))
- `duplicate_candidate_ordered` — CHECK ((customer_a_id < customer_b_id))
- `duplicate_candidate_score_range` — CHECK (((score >= (0)::numeric) AND (score <= (1)::numeric)))
- `duplicate_candidate_status_valid` — CHECK ((status = ANY (ARRAY['open'::text, 'merged'::text, 'dismissed'::text])))

### `customer_merge`

A merge event, with everything needed to reverse it. Merges get done wrong; the design assumes it.

| column | type | |
|---|---|---|
| `surviving_id` | uuid | not null |
| `merged_id` | uuid | not null |
| `performed_by` | uuid |  |
| `performed_at` | timestamp with time zone | not null · default `now()` |
| `reason_code` | text |  |
| `reason_text` | text |  |
| `moved_rows` | jsonb | not null · default `'{}'::jsonb` |
| `field_choices` | jsonb | not null · default `'{}'::jsonb` |
| `unmerged_at` | timestamp with time zone |  |
| `unmerged_by` | uuid |  |
| `unmerge_reason` | text |  |

References: `business`, `customer`, `app_user`, `customer`, `app_user`

Constraints:

- `customer_merge_distinct` — CHECK ((surviving_id <> merged_id))

### `garment_type`

Shirt, kurta, blouse, lehenga, blazer. Owner-configurable (AP-1); names are translated via the translation table, not duplicated per language.

| column | type | |
|---|---|---|
| `code` | text | not null |
| `name` | text | not null |
| `gender_applicability` | text | not null · default `'any'::text` |
| `default_turnaround_days` | smallint |  |
| `requires_trial` | boolean | not null · default `false` |
| `is_active` | boolean | not null · default `true` |
| `sort_order` | integer | not null · default `100` |

References: `business`

Constraints:

- `garment_type_gender_valid` — CHECK ((gender_applicability = ANY (ARRAY['any'::text, 'male'::text, 'female'::text])))

### `style_option_group`

Collar, cuff, pocket, neck, sleeve - the choices that make a garment this garment. Configurable per type.

| column | type | |
|---|---|---|
| `garment_type_id` | uuid | not null |
| `code` | text | not null |
| `label` | text | not null |
| `selection_type` | text | not null · default `'single'::text` |
| `is_required` | boolean | not null · default `false` |
| `sort_order` | integer | not null · default `100` |

References: `business`, `garment_type`

Constraints:

- `style_option_group_selection_valid` — CHECK ((selection_type = ANY (ARRAY['single'::text, 'multi'::text])))

### `style_option`

One choice within a style group, with its price and time effect.

| column | type | |
|---|---|---|
| `group_id` | uuid | not null |
| `code` | text | not null |
| `label` | text | not null |
| `price_delta` | app.money_amount | not null · default `0` |
| `extra_minutes` | integer | not null · default `0` |
| `is_active` | boolean | not null · default `true` |
| `sort_order` | integer | not null · default `100` |

References: `business`, `style_option_group`

### `price_list_item`

What a garment type costs in a given price list, and which tax classification applies to it.

| column | type | |
|---|---|---|
| `price_list_id` | uuid | not null |
| `garment_type_id` | uuid | not null |
| `unit_price` | app.money_amount | not null |
| `tax_code_id` | uuid |  |
| `is_active` | boolean | not null · default `true` |

References: `business`, `garment_type`, `price_list`, `tax_code`

Constraints:

- `price_list_item_price_non_negative` — CHECK (((unit_price)::numeric >= (0)::numeric))

### `measurement_template`

The field set for a garment type, versioned. Adding a custom field creates a new version; existing revisions and snapshots are untouched.

| column | type | |
|---|---|---|
| `garment_type_id` | uuid | not null |
| `version` | integer | not null · default `1` |
| `is_current` | boolean | not null · default `true` |
| `effective_from` | timestamp with time zone | not null · default `now()` |
| `note` | text |  |

References: `business`, `garment_type`

### `template_field`

One measurement field. Labels are translated via the translation table so a Hindi label is data, not a second column.

| column | type | |
|---|---|---|
| `template_id` | uuid | not null |
| `code` | text | not null |
| `label` | text | not null |
| `unit` | text | not null · default `'inch'::text` |
| `input_type` | text | not null · default `'decimal'::text` |
| `min_value` | numeric(8,3) |  |
| `max_value` | numeric(8,3) |  |
| `is_required` | boolean | not null · default `false` |
| `group_name` | text |  |
| `help_text` | text |  |
| `display_order` | integer | not null · default `100` |

References: `business`, `measurement_template`

Constraints:

- `template_field_input_valid` — CHECK ((input_type = ANY (ARRAY['decimal'::text, 'fraction'::text, 'integer'::text, 'select'::text, 'text'::text, 'boolean'::text])))
- `template_field_range_sane` — CHECK (((min_value IS NULL) OR (max_value IS NULL) OR (max_value >= min_value)))
- `template_field_unit_valid` — CHECK ((unit = ANY (ARRAY['inch'::text, 'cm'::text, 'none'::text])))

### `measurement_profile`

A named measurement set for one customer and garment type - "regular", "loose". A customer may have several.

| column | type | |
|---|---|---|
| `customer_id` | uuid | not null |
| `garment_type_id` | uuid | not null |
| `name` | text | not null · default `'regular'::text` |
| `is_default` | boolean | not null · default `true` |
| `notes` | text |  |

References: `business`, `customer`, `garment_type`

### `measurement_revision`

An append-only measurement event. Corrections create a revision; they never overwrite one. Two devices measuring the same person produce two revisions and lose nothing (§5.2).

| column | type | |
|---|---|---|
| `profile_id` | uuid | not null |
| `template_id` | uuid | not null |
| `revision_no` | integer | not null |
| `unit` | text | not null · default `'inch'::text` |
| `source` | text | not null · default `'measured_in_store'::text` |
| `taken_by` | uuid |  |
| `taken_at` | timestamp with time zone | not null · default `now()` |
| `notes` | text |  |
| `is_current` | boolean | not null · default `true` |
| `values_cache` | jsonb | not null · default `'{}'::jsonb` |

References: `business`, `measurement_profile`, `app_user`, `measurement_template`

Constraints:

- `measurement_revision_source_valid` — CHECK ((source = ANY (ARRAY['measured_in_store'::text, 'copied_from_previous'::text, 'from_sample_garment'::text, 'customer_provided'::text, 'alteration_derived'::text, 'imported'::text])))
- `measurement_revision_unit_valid` — CHECK ((unit = ANY (ARRAY['inch'::text, 'cm'::text])))

### `measurement_value`

The typed, queryable form of a revision. Analytics and the duplicate-scoring signal read these rows, not the JSON cache.

| column | type | |
|---|---|---|
| `revision_id` | uuid | not null |
| `field_code` | text | not null |
| `value_numeric` | numeric(8,3) |  |
| `value_text` | text |  |

References: `business`, `measurement_revision`

Constraints:

- `measurement_value_has_a_value` — CHECK (((value_numeric IS NOT NULL) OR (value_text IS NOT NULL)))

### `measurement_snapshot`

A deep copy of the measurements a garment was cut to, with no live link back to the profile. This table is why BR-01 is a guarantee rather than an intention.

| column | type | |
|---|---|---|
| `garment_id` | uuid |  |
| `source_revision_id` | uuid |  |
| `template_id` | uuid | not null |
| `template_version` | integer | not null |
| `unit` | text | not null |
| `source` | text | not null |
| `values_frozen` | jsonb | not null |
| `frozen_at` | timestamp with time zone | not null · default `now()` |
| `frozen_by` | uuid |  |
| `replaced_snapshot_id` | uuid |  |
| `replace_reason_code` | text |  |
| `replace_reason_text` | text |  |

References: `business`, `app_user`, `garment`, `measurement_snapshot`, `measurement_revision`, `measurement_template`

Constraints:

- `measurement_snapshot_values_not_empty` — CHECK ((values_frozen <> '{}'::jsonb))

### `attachment`

Every photo, signature and document in the system, attached to whatever it belongs to. Uploads are asynchronous by design.

| column | type | |
|---|---|---|
| `branch_id` | uuid |  |
| `entity_type` | text | not null |
| `entity_id` | uuid | not null |
| `kind` | text | not null |
| `storage_path` | text |  |
| `mime_type` | text |  |
| `byte_size` | bigint |  |
| `caption` | text |  |
| `captured_at` | timestamp with time zone |  |
| `uploaded_by` | uuid |  |
| `sync_state` | text | not null · default `'pending'::text` |

References: `branch`, `business`, `app_user`

Constraints:

- `attachment_kind_valid` — CHECK ((kind = ANY (ARRAY['reference_photo'::text, 'sample_garment'::text, 'fabric_photo'::text, 'style_reference'::text, 'delivery_proof'::text, 'signature'::text, 'document'::text, 'other'::text])))
- `attachment_sync_state_valid` — CHECK ((sync_state = ANY (ARRAY['pending'::text, 'uploading'::text, 'stored'::text, 'failed'::text])))

## 2.4 Orders, production, capacity

### `workflow_template`

The production route for a garment type. A blouse skips what a blazer needs, and adding embroidery as an outsourced stage is configuration, not code.

| column | type | |
|---|---|---|
| `garment_type_id` | uuid | not null |
| `version` | integer | not null · default `1` |
| `is_current` | boolean | not null · default `true` |
| `note` | text |  |

References: `business`, `garment_type`

### `workflow_stage`

One step of a production route, with the standard minutes the capacity engine schedules against.

| column | type | |
|---|---|---|
| `template_id` | uuid | not null |
| `code` | text | not null |
| `label` | text | not null |
| `sequence_no` | integer | not null |
| `standard_minutes` | integer | not null · default `0` |
| `buffer_minutes` | integer | not null · default `0` |
| `is_outsourced` | boolean | not null · default `false` |
| `is_wage_bearing` | boolean | not null · default `true` |
| `requires_qc` | boolean | not null · default `false` |

References: `business`, `workflow_template`

Constraints:

- `workflow_stage_minutes_non_negative` — CHECK ((standard_minutes >= 0))

### `priority_class`

Normal, urgent, VIP - owner-defined, with the weight each carries in the queue.

| column | type | |
|---|---|---|
| `code` | text | not null |
| `label` | text | not null |
| `queue_weight` | integer | not null · default `100` |
| `sort_order` | integer | not null · default `100` |
| `is_default` | boolean | not null · default `false` |
| `is_active` | boolean | not null · default `true` |

References: `business`

### `branch_calendar`

The normal working week for a branch. Non-working time is skipped when a promise date is calculated.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `weekday` | smallint | not null |
| `is_working` | boolean | not null · default `true` |
| `opens_at` | time without time zone |  |
| `closes_at` | time without time zone |  |

References: `branch`, `business`

Constraints:

- `branch_calendar_hours_sane` — CHECK (((opens_at IS NULL) OR (closes_at IS NULL) OR (closes_at > opens_at)))
- `branch_calendar_weekday_valid` — CHECK (((weekday >= 0) AND (weekday <= 6)))

### `calendar_exception`

Festival closures, half-days and the occasional extra working Sunday.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `exception_date` | date | not null |
| `is_working` | boolean | not null · default `false` |
| `opens_at` | time without time zone |  |
| `closes_at` | time without time zone |  |
| `reason` | text |  |

References: `branch`, `business`

### `staff_capacity`

How many minutes a person actually has in a day, and how fast they work relative to standard.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `user_id` | uuid | not null |
| `minutes_per_day` | integer | not null |
| `efficiency_factor` | numeric(4,2) | not null · default `1.00` |
| `effective_from` | date | not null |
| `effective_to` | date |  |

References: `branch`, `business`, `app_user`

Constraints:

- `staff_capacity_efficiency_sane` — CHECK (((efficiency_factor > (0)::numeric) AND (efficiency_factor <= (3)::numeric)))
- `staff_capacity_minutes_positive` — CHECK ((minutes_per_day > 0))
- `staff_capacity_no_overlap` — EXCLUDE USING gist (user_id WITH =, branch_id WITH =, daterange(effective_from, effective_to) WITH &&) WHERE ((deleted_at IS NULL))
- `staff_capacity_range_valid` — CHECK (((effective_to IS NULL) OR (effective_to > effective_from)))

### `staff_skill`

Who may be scheduled for what. Work is never scheduled into a pool that cannot do it - a blouse does not go to a pool rated only for trousers.

| column | type | |
|---|---|---|
| `user_id` | uuid | not null |
| `garment_type_id` | uuid | not null |
| `skill_level` | smallint | not null · default `3` |
| `is_eligible` | boolean | not null · default `true` |

References: `business`, `garment_type`, `app_user`

Constraints:

- `staff_skill_level_valid` — CHECK (((skill_level >= 1) AND (skill_level <= 5)))

### `sales_order`

The commercial agreement. Its delivery status is derived from its garments, never set by hand (BR-06); its totals are derived from its lines (AP-8).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `order_no` | text | not null |
| `customer_id` | uuid | not null |
| `bill_to_household_id` | uuid |  |
| `order_date` | date | not null · default `CURRENT_DATE` |
| `priority_class_id` | uuid |  |
| `source` | text | not null · default `'walk_in'::text` |
| `price_list_id` | uuid |  |
| `collection_policy` | text | not null · default `'on_delivery'::text` |
| `lifecycle` | text | not null · default `'draft'::text` |
| `confirmed_at` | timestamp with time zone |  |
| `cancelled_at` | timestamp with time zone |  |
| `cancel_reason_code` | text |  |
| `cancel_reason_text` | text |  |
| `notes` | text |  |

References: `household`, `branch`, `business`, `customer`, `price_list`, `priority_class`

Constraints:

- `sales_order_cancel_coherent` — CHECK ((((lifecycle = 'cancelled'::text) AND (cancelled_at IS NOT NULL)) OR (lifecycle <> 'cancelled'::text)))
- `sales_order_collection_policy_valid` — CHECK ((collection_policy = ANY (ARRAY['on_delivery'::text, 'on_completion'::text])))
- `sales_order_confirm_coherent` — CHECK ((((lifecycle = 'draft'::text) AND (confirmed_at IS NULL)) OR (lifecycle <> 'draft'::text)))
- `sales_order_lifecycle_valid` — CHECK ((lifecycle = ANY (ARRAY['draft'::text, 'confirmed'::text, 'cancelled'::text])))
- `sales_order_source_valid` — CHECK ((source = ANY (ARRAY['walk_in'::text, 'phone'::text, 'whatsapp'::text, 'online'::text, 'other'::text])))

### `order_item`

A priced line. Quantity here becomes that many garment rows (BR-07): the line is commercial, the garment is physical.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `order_id` | uuid | not null |
| `garment_type_id` | uuid | not null |
| `quantity` | integer | not null |
| `unit_price` | app.money_amount | not null |
| `discount_amount` | app.money_amount | not null · default `0` |
| `discount_reason_code` | text |  |
| `tax_code_id` | uuid |  |
| `line_no` | integer | not null · default `1` |
| `notes` | text |  |

References: `branch`, `business`, `garment_type`, `sales_order`, `tax_code`

Constraints:

- `order_item_discount_non_negative` — CHECK (((discount_amount)::numeric >= (0)::numeric))
- `order_item_discount_within_line` — CHECK (((discount_amount)::numeric <= ((unit_price)::numeric * (quantity)::numeric)))
- `order_item_price_non_negative` — CHECK (((unit_price)::numeric >= (0)::numeric))
- `order_item_quantity_positive` — CHECK ((quantity >= 1))

### `garment`

One physical piece. Everything on the shop floor - assignment, capacity, wages, delivery - is counted here, not on the order.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `order_item_id` | uuid | not null |
| `garment_no` | text | not null |
| `piece_no` | integer | not null · default `1` |
| `allocated_amount` | app.money_amount | not null · default `0` |
| `allocated_tax_amount` | app.money_amount | not null · default `0` |
| `measurement_snapshot_id` | uuid |  |
| `fabric_source` | text | not null · default `'customer'::text` |
| `lifecycle` | text | not null · default `'pending'::text` |
| `current_stage_id` | uuid |  |
| `promised_date_original` | date |  |
| `promised_date_current` | date |  |
| `trial_required` | boolean | not null · default `false` |
| `ready_at` | timestamp with time zone |  |
| `delivered_at` | timestamp with time zone |  |
| `cancelled_at` | timestamp with time zone |  |
| `cancel_reason_code` | text |  |
| `notes` | text |  |

References: `branch`, `business`, `workflow_stage`, `measurement_snapshot`, `order_item`

Constraints:

- `garment_allocation_non_negative` — CHECK ((((allocated_amount)::numeric >= (0)::numeric) AND ((allocated_tax_amount)::numeric >= (0)::numeric)))
- `garment_cancelled_coherent` — CHECK ((((lifecycle = 'cancelled'::text) AND (cancelled_at IS NOT NULL)) OR (lifecycle <> 'cancelled'::text)))
- `garment_delivered_coherent` — CHECK ((((lifecycle = 'delivered'::text) AND (delivered_at IS NOT NULL)) OR (lifecycle <> 'delivered'::text)))
- `garment_fabric_source_valid` — CHECK ((fabric_source = ANY (ARRAY['customer'::text, 'shop'::text, 'mixed'::text])))
- `garment_lifecycle_valid` — CHECK ((lifecycle = ANY (ARRAY['pending'::text, 'in_production'::text, 'ready'::text, 'delivered'::text, 'cancelled'::text])))

### `garment_style_option`

The style choices for one garment, with the price and time they carried when the order was confirmed.

| column | type | |
|---|---|---|
| `garment_id` | uuid | not null |
| `group_id` | uuid | not null |
| `option_id` | uuid | not null |
| `price_delta_applied` | app.money_amount | not null · default `0` |
| `extra_minutes_applied` | integer | not null · default `0` |

References: `business`, `garment`, `style_option_group`, `style_option`

### `job_card`

A work order for one tailor, batching one or more garments. Printed with a QR code so a manager can move stages for a tailor who does not use a phone.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `job_no` | text | not null |
| `assigned_to` | uuid |  |
| `kind` | text | not null · default `'production'::text` |
| `issued_at` | timestamp with time zone |  |
| `due_at` | timestamp with time zone |  |
| `returned_at` | timestamp with time zone |  |
| `status` | text | not null · default `'open'::text` |
| `notes` | text |  |

References: `app_user`, `branch`, `business`

Constraints:

- `job_card_kind_valid` — CHECK ((kind = ANY (ARRAY['production'::text, 'rework'::text])))
- `job_card_status_valid` — CHECK ((status = ANY (ARRAY['open'::text, 'issued'::text, 'in_progress'::text, 'returned'::text, 'closed'::text, 'cancelled'::text])))

### `job_card_garment`

Which garments a job card carries. A link table rather than a column on garment, because a garment returning for rework joins a second job card and the history of both must survive.

| column | type | |
|---|---|---|
| `job_card_id` | uuid | not null |
| `garment_id` | uuid | not null |
| `is_active` | boolean | not null · default `true` |
| `removed_at` | timestamp with time zone |  |

References: `business`, `garment`, `job_card`

### `production_task`

One stage of one garment on one job card. The row that feeds capacity, wages, bottleneck reporting and actual-versus-standard calibration.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `job_card_id` | uuid | not null |
| `garment_id` | uuid | not null |
| `stage_id` | uuid | not null |
| `sequence_no` | integer | not null |
| `assignee_id` | uuid |  |
| `standard_minutes` | integer | not null · default `0` |
| `planned_start` | timestamp with time zone |  |
| `planned_end` | timestamp with time zone |  |
| `actual_start` | timestamp with time zone |  |
| `actual_end` | timestamp with time zone |  |
| `status` | text | not null · default `'pending'::text` |
| `piece_rate` | app.money_amount | not null · default `0` |
| `notes` | text |  |

References: `app_user`, `branch`, `business`, `garment`, `job_card`, `workflow_stage`

Constraints:

- `production_task_actual_order` — CHECK (((actual_end IS NULL) OR (actual_start IS NULL) OR (actual_end >= actual_start)))
- `production_task_done_has_end` — CHECK (((status <> 'done'::text) OR (actual_end IS NOT NULL)))
- `production_task_minutes_non_negative` — CHECK ((standard_minutes >= 0))
- `production_task_planned_order` — CHECK (((planned_end IS NULL) OR (planned_start IS NULL) OR (planned_end >= planned_start)))
- `production_task_rate_non_negative` — CHECK (((piece_rate)::numeric >= (0)::numeric))
- `production_task_status_valid` — CHECK ((status = ANY (ARRAY['pending'::text, 'in_progress'::text, 'done'::text, 'skipped'::text, 'cancelled'::text])))

### `trial_event`

A fitting. Repeatable: a garment may go to trial more than once, and each attempt is kept.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `garment_id` | uuid | not null |
| `attempt_no` | integer | not null · default `1` |
| `scheduled_at` | timestamp with time zone |  |
| `occurred_at` | timestamp with time zone |  |
| `outcome` | text |  |
| `notes` | text |  |

References: `branch`, `business`, `garment`

Constraints:

- `trial_event_outcome_valid` — CHECK ((outcome = ANY (ARRAY['fits'::text, 'alterations_noted'::text, 'customer_absent'::text, 'rescheduled'::text])))

### `alteration`

A rework job in its own right, not an edit to the original (BR-08). Fault party is what makes rework rate an honest quality signal.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `garment_id` | uuid | not null |
| `source_job_card_id` | uuid |  |
| `rework_job_card_id` | uuid |  |
| `reason_code` | text | not null |
| `reason_text` | text |  |
| `fault_party` | text | not null |
| `chargeable` | boolean | not null · default `false` |
| `charge_amount` | app.money_amount | not null · default `0` |
| `due_date` | date |  |
| `status` | text | not null · default `'open'::text` |
| `completed_at` | timestamp with time zone |  |

References: `branch`, `business`, `garment`, `job_card`, `job_card`

Constraints:

- `alteration_charge_coherent` — CHECK (((chargeable AND ((charge_amount)::numeric > (0)::numeric)) OR ((NOT chargeable) AND ((charge_amount)::numeric = (0)::numeric))))
- `alteration_charge_non_negative` — CHECK (((charge_amount)::numeric >= (0)::numeric))
- `alteration_fault_party_valid` — CHECK ((fault_party = ANY (ARRAY['shop'::text, 'customer_preference'::text, 'fabric'::text, 'unknown'::text])))
- `alteration_status_valid` — CHECK ((status = ANY (ARRAY['open'::text, 'in_progress'::text, 'done'::text, 'cancelled'::text])))

### `date_override`

A promise date changed by a person, with what it was, what it became and why (BR-10). Override frequency is reported per user alongside their on-time rate.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `entity_type` | text | not null |
| `entity_id` | uuid | not null |
| `date_kind` | text | not null · default `'promised_delivery'::text` |
| `original_date` | date | not null |
| `new_date` | date | not null |
| `user_id` | uuid |  |
| `reason_code` | text | not null |
| `reason_text` | text |  |
| `approved_by` | uuid |  |
| `overridden_at` | timestamp with time zone | not null · default `now()` |

References: `app_user`, `branch`, `business`, `app_user`

Constraints:

- `date_override_actually_changes` — CHECK ((new_date <> original_date))
- `date_override_entity_valid` — CHECK ((entity_type = ANY (ARRAY['garment'::text, 'order'::text, 'alteration'::text])))
- `date_override_kind_valid` — CHECK ((date_kind = ANY (ARRAY['promised_delivery'::text, 'trial'::text, 'alteration_due'::text])))

### `delivery_note`

One handover event. An order may have several: partial delivery is the normal case, not an exception (§4).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `note_no` | text | not null |
| `order_id` | uuid | not null |
| `delivered_at` | timestamp with time zone | not null · default `now()` |
| `delivered_by` | uuid |  |
| `received_by_name` | text |  |
| `acknowledgment_id` | uuid |  |
| `collection_overridden` | boolean | not null · default `false` |
| `override_reason_code` | text |  |
| `notes` | text |  |

References: `attachment`, `branch`, `business`, `app_user`, `sales_order`

### `delivery_line`

Which garments moved on a handover, and in which direction. The rule that a garment cannot go out twice without a return in between is a state-machine invariant enforced by the delivery service in P2, with its own tests.

| column | type | |
|---|---|---|
| `delivery_note_id` | uuid | not null |
| `garment_id` | uuid | not null |
| `direction` | text | not null · default `'out'::text` |
| `return_reason_code` | text |  |

References: `business`, `delivery_note`, `garment`

Constraints:

- `delivery_line_direction_valid` — CHECK ((direction = ANY (ARRAY['out'::text, 'return'::text])))

## 2.5 Finance

### `account`

The chart of accounts, seeded per business. Cash is one account; per-branch cash comes from the branch dimension on the journal line, not from a separate account per outlet.

| column | type | |
|---|---|---|
| `code` | text | not null |
| `name` | text | not null |
| `account_type` | text | not null |
| `normal_balance` | text | not null |
| `is_contra` | boolean | not null · default `false` |
| `parent_id` | uuid |  |
| `is_system` | boolean | not null · default `false` |
| `is_active` | boolean | not null · default `true` |
| `description` | text |  |

References: `business`, `account`

Constraints:

- `account_normal_balance_valid` — CHECK ((normal_balance = ANY (ARRAY['debit'::text, 'credit'::text])))
- `account_not_own_parent` — CHECK (((parent_id IS NULL) OR (parent_id <> id)))
- `account_type_valid` — CHECK ((account_type = ANY (ARRAY['asset'::text, 'liability'::text, 'equity'::text, 'revenue'::text, 'direct_cost'::text, 'expense'::text, 'control'::text])))

### `accounting_period`

A month or a year. A locked period accepts no postings: corrections go to the current period as reversals (§4).

| column | type | |
|---|---|---|
| `code` | text | not null |
| `starts_on` | date | not null |
| `ends_on` | date | not null |
| `status` | text | not null · default `'open'::text` |
| `closed_at` | timestamp with time zone |  |
| `locked_at` | timestamp with time zone |  |
| `locked_by` | uuid |  |

References: `business`, `app_user`

Constraints:

- `accounting_period_locked_coherent` — CHECK (((status <> 'locked'::text) OR (locked_at IS NOT NULL)))
- `accounting_period_no_overlap` — EXCLUDE USING gist (business_id WITH =, daterange(starts_on, ends_on, '[]'::text) WITH &&) WHERE ((deleted_at IS NULL))
- `accounting_period_range_valid` — CHECK ((ends_on >= starts_on))
- `accounting_period_status_valid` — CHECK ((status = ANY (ARRAY['open'::text, 'closed'::text, 'locked'::text])))

### `journal_entry`

One balanced accounting event. Immutable once posted: UPDATE and DELETE are revoked from the application role in WP-5, and corrections are reversals (BR-04).

| column | type | |
|---|---|---|
| `branch_id` | uuid |  |
| `entry_no` | text |  |
| `period_id` | uuid |  |
| `entry_date` | date | not null · default `CURRENT_DATE` |
| `source_doc_type` | text | not null |
| `source_doc_id` | uuid |  |
| `memo` | text |  |
| `reversal_of_id` | uuid |  |
| `reversal_reason_code` | text |  |
| `reversal_reason_text` | text |  |
| `posted_at` | timestamp with time zone | not null · default `now()` |
| `posted_by` | uuid |  |

References: `branch`, `business`, `accounting_period`, `app_user`, `journal_entry`

Constraints:

- `journal_entry_not_own_reversal` — CHECK (((reversal_of_id IS NULL) OR (reversal_of_id <> id)))
- `journal_entry_reversal_has_reason` — CHECK (((reversal_of_id IS NULL) OR (reversal_reason_code IS NOT NULL)))
- `journal_entry_source_valid` — CHECK ((source_doc_type = ANY (ARRAY['invoice'::text, 'payment'::text, 'credit_note'::text, 'delivery'::text, 'stock_issue'::text, 'stock_adjustment'::text, 'purchase_bill'::text, 'expense'::text, 'wage_accrual'::text, 'wage_payout'::text, 'cash_session'::text, 'opening_balance'::text, 'manual_adjustment'::text])))

### `journal_line`

One side of an entry, carrying the dimensions that make reporting derivable: branch, customer, garment, staff, supplier, stock item.

| column | type | |
|---|---|---|
| `branch_id` | uuid |  |
| `entry_id` | uuid | not null |
| `account_id` | uuid | not null |
| `line_no` | integer | not null · default `1` |
| `debit` | app.money_amount | not null · default `0` |
| `credit` | app.money_amount | not null · default `0` |
| `customer_id` | uuid |  |
| `supplier_id` | uuid |  |
| `garment_id` | uuid |  |
| `staff_id` | uuid |  |
| `stock_item_id` | uuid |  |
| `memo` | text |  |

References: `account`, `branch`, `business`, `customer`, `journal_entry`, `garment`, `app_user`

Constraints:

- `journal_line_amounts_non_negative` — CHECK ((((debit)::numeric >= (0)::numeric) AND ((credit)::numeric >= (0)::numeric)))
- `journal_line_exactly_one_side` — CHECK (((((debit)::numeric > (0)::numeric) AND ((credit)::numeric = (0)::numeric)) OR (((credit)::numeric > (0)::numeric) AND ((debit)::numeric = (0)::numeric))))

### `cash_session`

An open drawer for one branch, one day, one person. Closing it requires a counted amount - a close without a count is not a close.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `user_id` | uuid | not null |
| `opened_at` | timestamp with time zone | not null · default `now()` |
| `opening_float` | app.money_amount | not null · default `0` |
| `closed_at` | timestamp with time zone |  |
| `expected_amount` | app.money_amount |  |
| `counted_amount` | app.money_amount |  |
| `difference_amount` | app.money_amount |  |
| `status` | text | not null · default `'open'::text` |
| `notes` | text |  |
| `journal_entry_id` | uuid |  |

References: `branch`, `business`, `journal_entry`, `app_user`

Constraints:

- `cash_session_closed_coherent` — CHECK (((status = 'open'::text) OR ((closed_at IS NOT NULL) AND (counted_amount IS NOT NULL))))
- `cash_session_float_non_negative` — CHECK (((opening_float)::numeric >= (0)::numeric))
- `cash_session_status_valid` — CHECK ((status = ANY (ARRAY['open'::text, 'closed'::text, 'reconciled'::text])))

### `invoice`

A tax document. Totals are stored because the document must reprint identically years later - this is a record, not a cache (contrast sales_order, whose totals are derived).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `invoice_no` | text | not null |
| `doc_type` | text | not null · default `'tax_invoice'::text` |
| `order_id` | uuid |  |
| `delivery_note_id` | uuid |  |
| `customer_id` | uuid | not null |
| `bill_to_household_id` | uuid |  |
| `invoice_date` | date | not null · default `CURRENT_DATE` |
| `tax_profile_id` | uuid |  |
| `place_of_supply` | text |  |
| `pricing_mode` | text | not null · default `'exclusive'::text` |
| `subtotal_amount` | app.money_amount | not null · default `0` |
| `discount_amount` | app.money_amount | not null · default `0` |
| `taxable_amount` | app.money_amount | not null · default `0` |
| `tax_amount` | app.money_amount | not null · default `0` |
| `rounding_amount` | app.money_amount | not null · default `0` |
| `total_amount` | app.money_amount | not null · default `0` |
| `is_draft_watermarked` | boolean | not null · default `true` |
| `status` | text | not null · default `'issued'::text` |
| `journal_entry_id` | uuid |  |
| `notes` | text |  |

References: `household`, `branch`, `business`, `customer`, `delivery_note`, `journal_entry`, `sales_order`, `tax_profile`

Constraints:

- `invoice_amounts_non_negative` — CHECK ((((subtotal_amount)::numeric >= (0)::numeric) AND ((discount_amount)::numeric >= (0)::numeric) AND ((taxable_amount)::numeric >= (0)::numeric) AND ((tax_amount)::numeric >= (0)::numeric) AND ((total_amount)::numeric >= (0)::numeric)))
- `invoice_doc_type_valid` — CHECK ((doc_type = ANY (ARRAY['tax_invoice'::text, 'bill_of_supply'::text, 'estimate'::text])))
- `invoice_pricing_mode_valid` — CHECK ((pricing_mode = ANY (ARRAY['inclusive'::text, 'exclusive'::text])))
- `invoice_status_valid` — CHECK ((status = ANY (ARRAY['draft'::text, 'issued'::text, 'cancelled'::text])))

### `invoice_line`

One printed line, with the tax rate that applied at issue copied in rather than looked up later (BR-15).

| column | type | |
|---|---|---|
| `invoice_id` | uuid | not null |
| `line_no` | integer | not null · default `1` |
| `garment_id` | uuid |  |
| `order_item_id` | uuid |  |
| `stock_item_id` | uuid |  |
| `description` | text | not null |
| `hsn_sac` | text |  |
| `quantity` | numeric(12,3) | not null · default `1` |
| `unit_price` | app.money_amount | not null · default `0` |
| `discount_amount` | app.money_amount | not null · default `0` |
| `tax_code_id` | uuid |  |
| `tax_rate_id` | uuid |  |
| `tax_percent` | app.rate_percent | not null · default `0` |
| `taxable_amount` | app.money_amount | not null · default `0` |
| `tax_amount` | app.money_amount | not null · default `0` |
| `total_amount` | app.money_amount | not null · default `0` |

References: `business`, `garment`, `invoice`, `order_item`, `tax_code`, `tax_rate`

Constraints:

- `invoice_line_amounts_non_negative` — CHECK ((((unit_price)::numeric >= (0)::numeric) AND ((discount_amount)::numeric >= (0)::numeric) AND ((taxable_amount)::numeric >= (0)::numeric) AND ((tax_amount)::numeric >= (0)::numeric) AND ((total_amount)::numeric >= (0)::numeric)))
- `invoice_line_quantity_positive` — CHECK ((quantity > (0)::numeric))

### `credit_note`

The correction document for an issued invoice. An invoice is never edited; a credit note says what changed and why.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `note_no` | text | not null |
| `invoice_id` | uuid | not null |
| `customer_id` | uuid | not null |
| `note_date` | date | not null · default `CURRENT_DATE` |
| `reason_code` | text | not null |
| `reason_text` | text |  |
| `taxable_amount` | app.money_amount | not null · default `0` |
| `tax_amount` | app.money_amount | not null · default `0` |
| `total_amount` | app.money_amount | not null · default `0` |
| `journal_entry_id` | uuid |  |

References: `branch`, `business`, `customer`, `invoice`, `journal_entry`

Constraints:

- `credit_note_amounts_non_negative` — CHECK ((((taxable_amount)::numeric >= (0)::numeric) AND ((tax_amount)::numeric >= (0)::numeric) AND ((total_amount)::numeric >= (0)::numeric)))

### `payment_mode`

Cash, UPI, card, bank transfer, cheque - each mapped to an account. Adding "Paytm QR - Branch 2" is configuration, not a release (AP-1).

| column | type | |
|---|---|---|
| `code` | text | not null |
| `label` | text | not null |
| `account_id` | uuid | not null |
| `requires_reference` | boolean | not null · default `false` |
| `is_cash` | boolean | not null · default `false` |
| `settles_immediately` | boolean | not null · default `true` |
| `is_active` | boolean | not null · default `true` |
| `sort_order` | integer | not null · default `100` |

References: `account`, `business`

### `payment`

Money in or out, with an immutable history. Correction is by reversal only; the id is client-generated so an offline replay is a no-op (§5.2).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `payment_no` | text |  |
| `direction` | text | not null |
| `customer_id` | uuid |  |
| `supplier_id` | uuid |  |
| `staff_id` | uuid |  |
| `amount` | app.money_amount | not null |
| `mode_id` | uuid | not null |
| `reference` | text |  |
| `occurred_at` | timestamp with time zone | not null · default `now()` |
| `received_by` | uuid |  |
| `cash_session_id` | uuid |  |
| `status` | text | not null · default `'posted'::text` |
| `reversal_of_id` | uuid |  |
| `reversal_reason_code` | text |  |
| `journal_entry_id` | uuid |  |
| `notes` | text |  |

References: `branch`, `business`, `cash_session`, `customer`, `journal_entry`, `payment_mode`, `app_user`, `payment`, `app_user`

Constraints:

- `payment_amount_positive` — CHECK (((amount)::numeric > (0)::numeric))
- `payment_direction_valid` — CHECK ((direction = ANY (ARRAY['in'::text, 'out'::text])))
- `payment_has_a_counterparty` — CHECK (((customer_id IS NOT NULL) OR (supplier_id IS NOT NULL) OR (staff_id IS NOT NULL)))
- `payment_not_own_reversal` — CHECK (((reversal_of_id IS NULL) OR (reversal_of_id <> id)))
- `payment_reversal_has_reason` — CHECK (((reversal_of_id IS NULL) OR (reversal_reason_code IS NOT NULL)))
- `payment_status_valid` — CHECK ((status = ANY (ARRAY['posted'::text, 'reversed'::text])))

### `payment_allocation`

Which debt a payment settles. Its own table because one payment may settle four family orders, and one order may take six payments.

| column | type | |
|---|---|---|
| `payment_id` | uuid | not null |
| `invoice_id` | uuid |  |
| `order_id` | uuid |  |
| `amount` | app.money_amount | not null |
| `allocated_at` | timestamp with time zone | not null · default `now()` |

References: `business`, `invoice`, `sales_order`, `payment`

Constraints:

- `payment_allocation_amount_positive` — CHECK (((amount)::numeric > (0)::numeric))
- `payment_allocation_target_exactly_one` — CHECK ((((invoice_id IS NOT NULL) AND (order_id IS NULL)) OR ((invoice_id IS NULL) AND (order_id IS NOT NULL))))

### `supplier`

Who you buy fabric, trims and services from.

| column | type | |
|---|---|---|
| `name` | text | not null |
| `gstin` | text |  |
| `phone_e164` | text |  |
| `email` | text |  |
| `address_line1` | text |  |
| `city` | text |  |
| `state_code` | text |  |
| `notes` | text |  |
| `is_active` | boolean | not null · default `true` |

References: `business`

### `purchase_bill`

A supplier invoice. Creates a payable; paying it is a separate event.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `supplier_id` | uuid | not null |
| `bill_no` | text | not null |
| `bill_date` | date | not null · default `CURRENT_DATE` |
| `due_date` | date |  |
| `subtotal_amount` | app.money_amount | not null · default `0` |
| `tax_amount` | app.money_amount | not null · default `0` |
| `total_amount` | app.money_amount | not null · default `0` |
| `status` | text | not null · default `'open'::text` |
| `journal_entry_id` | uuid |  |
| `notes` | text |  |

References: `branch`, `business`, `journal_entry`, `supplier`

Constraints:

- `purchase_bill_amounts_non_negative` — CHECK ((((subtotal_amount)::numeric >= (0)::numeric) AND ((tax_amount)::numeric >= (0)::numeric) AND ((total_amount)::numeric >= (0)::numeric)))
- `purchase_bill_status_valid` — CHECK ((status = ANY (ARRAY['draft'::text, 'open'::text, 'paid'::text, 'cancelled'::text])))

### `purchase_bill_line`

What was bought, at what cost - the source of weighted average cost for stock valuation.

| column | type | |
|---|---|---|
| `purchase_bill_id` | uuid | not null |
| `line_no` | integer | not null · default `1` |
| `stock_item_id` | uuid |  |
| `description` | text | not null |
| `quantity` | app.quantity | not null |
| `unit_cost` | app.money_amount | not null · default `0` |
| `tax_code_id` | uuid |  |
| `tax_amount` | app.money_amount | not null · default `0` |
| `total_amount` | app.money_amount | not null · default `0` |

References: `business`, `purchase_bill`, `tax_code`

Constraints:

- `purchase_bill_line_quantity_positive` — CHECK (((quantity)::numeric > (0)::numeric))

### `expense`

Rent, electricity, transport - and the one route by which customer-owned material can ever touch the books: compensation for loss, entered by a person with a reason (BR-05).

| column | type | |
|---|---|---|
| `branch_id` | uuid |  |
| `account_id` | uuid | not null |
| `supplier_id` | uuid |  |
| `expense_date` | date | not null · default `CURRENT_DATE` |
| `amount` | app.money_amount | not null |
| `tax_amount` | app.money_amount | not null · default `0` |
| `paid_via_mode_id` | uuid |  |
| `reference` | text |  |
| `description` | text |  |
| `journal_entry_id` | uuid |  |

References: `account`, `branch`, `business`, `journal_entry`, `payment_mode`, `supplier`

Constraints:

- `expense_amount_positive` — CHECK (((amount)::numeric > (0)::numeric))

### `wage_scheme`

How one person is paid, over a period. All four schemes are supported; a salaried floor still attributes cost per garment or gross profit by garment type is fiction.

| column | type | |
|---|---|---|
| `user_id` | uuid | not null |
| `scheme_type` | text | not null |
| `monthly_amount` | app.money_amount | not null · default `0` |
| `effective_from` | date | not null |
| `effective_to` | date |  |

References: `business`, `app_user`

Constraints:

- `wage_scheme_monthly_non_negative` — CHECK (((monthly_amount)::numeric >= (0)::numeric))
- `wage_scheme_no_overlap` — EXCLUDE USING gist (user_id WITH =, daterange(effective_from, effective_to) WITH &&) WHERE ((deleted_at IS NULL))
- `wage_scheme_range_valid` — CHECK (((effective_to IS NULL) OR (effective_to > effective_from)))
- `wage_scheme_type_valid` — CHECK ((scheme_type = ANY (ARRAY['piece_rate'::text, 'salary'::text, 'hybrid'::text, 'contractor'::text])))

### `wage_rate`

What a stage of a garment type earns. Effective-dated, so raising a rate never restates what past work paid.

| column | type | |
|---|---|---|
| `user_id` | uuid |  |
| `garment_type_id` | uuid | not null |
| `stage_id` | uuid |  |
| `amount` | app.money_amount | not null |
| `effective_from` | date | not null |
| `effective_to` | date |  |

References: `business`, `garment_type`, `workflow_stage`, `app_user`

Constraints:

- `wage_rate_amount_non_negative` — CHECK (((amount)::numeric >= (0)::numeric))
- `wage_rate_range_valid` — CHECK (((effective_to IS NULL) OR (effective_to > effective_from)))

### `wage_payout`

A payout run for one person over one period. Locking it freezes what was owed; paying it is a separate event with its own payment row.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `payout_no` | text |  |
| `user_id` | uuid | not null |
| `period_start` | date | not null |
| `period_end` | date | not null |
| `gross_amount` | app.money_amount | not null · default `0` |
| `deduction_amount` | app.money_amount | not null · default `0` |
| `advance_recovered` | app.money_amount | not null · default `0` |
| `net_amount` | app.money_amount | not null · default `0` |
| `status` | text | not null · default `'draft'::text` |
| `locked_at` | timestamp with time zone |  |
| `paid_at` | timestamp with time zone |  |
| `payment_id` | uuid |  |
| `journal_entry_id` | uuid |  |

References: `branch`, `business`, `journal_entry`, `payment`, `app_user`

Constraints:

- `wage_payout_amounts_non_negative` — CHECK ((((gross_amount)::numeric >= (0)::numeric) AND ((deduction_amount)::numeric >= (0)::numeric) AND ((advance_recovered)::numeric >= (0)::numeric)))
- `wage_payout_locked_coherent` — CHECK (((status = 'draft'::text) OR (locked_at IS NOT NULL)))
- `wage_payout_period_valid` — CHECK ((period_end >= period_start))
- `wage_payout_status_valid` — CHECK ((status = ANY (ARRAY['draft'::text, 'locked'::text, 'paid'::text, 'cancelled'::text])))

### `wage_entry`

What one person earned for one piece of work. Accrues when the task completes, independent of whether the customer has paid (BR-16).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `user_id` | uuid | not null |
| `production_task_id` | uuid |  |
| `garment_id` | uuid |  |
| `alteration_id` | uuid |  |
| `amount` | app.money_amount | not null |
| `earned_at` | timestamp with time zone | not null · default `now()` |
| `kind` | text | not null · default `'piece_rate'::text` |
| `reason_code` | text |  |
| `reason_text` | text |  |
| `payout_id` | uuid |  |
| `journal_entry_id` | uuid |  |

References: `alteration`, `branch`, `business`, `garment`, `journal_entry`, `wage_payout`, `production_task`, `app_user`

Constraints:

- `wage_entry_kind_valid` — CHECK ((kind = ANY (ARRAY['piece_rate'::text, 'salary'::text, 'bonus'::text, 'adjustment'::text, 'penalty'::text])))
- `wage_entry_negative_needs_reason` — CHECK ((((amount)::numeric >= (0)::numeric) OR ((kind = ANY (ARRAY['adjustment'::text, 'penalty'::text])) AND (reason_code IS NOT NULL))))

### `staff_advance`

Money advanced to a karigar against future work, recovered from payouts. Over-recovery is impossible by constraint.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `user_id` | uuid | not null |
| `amount` | app.money_amount | not null |
| `recovered_amount` | app.money_amount | not null · default `0` |
| `given_at` | timestamp with time zone | not null · default `now()` |
| `payment_id` | uuid |  |
| `status` | text | not null · default `'outstanding'::text` |
| `notes` | text |  |

References: `branch`, `business`, `payment`, `app_user`

Constraints:

- `staff_advance_amount_positive` — CHECK (((amount)::numeric > (0)::numeric))
- `staff_advance_not_over_recovered` — CHECK (((recovered_amount)::numeric <= (amount)::numeric))
- `staff_advance_recovered_non_negative` — CHECK (((recovered_amount)::numeric >= (0)::numeric))
- `staff_advance_status_valid` — CHECK ((status = ANY (ARRAY['outstanding'::text, 'partly_recovered'::text, 'recovered'::text, 'written_off'::text])))

## 2.6 Inventory and customer material

### `stock_item`

Something the business owns and consumes: fabric by the metre, trims by the piece. Ownership is implicit in this table - customer cloth lives in customer_material and never here.

| column | type | |
|---|---|---|
| `code` | text | not null |
| `name` | text | not null |
| `kind` | text | not null · default `'fabric'::text` |
| `uom` | text | not null · default `'metre'::text` |
| `valuation_method` | text | not null · default `'weighted_average'::text` |
| `tax_code_id` | uuid |  |
| `reorder_level` | app.quantity |  |
| `allow_negative` | boolean | not null · default `true` |
| `is_active` | boolean | not null · default `true` |
| `notes` | text |  |

References: `business`, `tax_code`

Constraints:

- `stock_item_kind_valid` — CHECK ((kind = ANY (ARRAY['fabric'::text, 'trim'::text, 'consumable'::text, 'finished_good'::text])))
- `stock_item_uom_valid` — CHECK ((uom = ANY (ARRAY['metre'::text, 'piece'::text, 'roll'::text, 'metre_square'::text, 'kilogram'::text, 'set'::text])))
- `stock_item_valuation_valid` — CHECK ((valuation_method = ANY (ARRAY['weighted_average'::text, 'fifo'::text])))

### `stock_movement`

Append-only. Balances are derived from these rows; no screen edits a balance, and a correction is an adjustment movement with a reason that stays visible forever (BR-09).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `stock_item_id` | uuid | not null |
| `movement_type` | text | not null |
| `qty_signed` | app.quantity | not null |
| `unit_cost` | app.money_amount | not null · default `0` |
| `total_cost` | app.money_amount | not null · default `0` |
| `ref_doc_type` | text |  |
| `ref_doc_id` | uuid |  |
| `garment_id` | uuid |  |
| `reason_code` | text |  |
| `reason_text` | text |  |
| `occurred_at` | timestamp with time zone | not null · default `now()` |
| `journal_entry_id` | uuid |  |

References: `branch`, `business`, `garment`, `journal_entry`, `stock_item`

Constraints:

- `stock_movement_adjustment_needs_reason` — CHECK (((movement_type <> ALL (ARRAY['adjustment'::text, 'wastage'::text])) OR (reason_code IS NOT NULL)))
- `stock_movement_cost_non_negative` — CHECK (((unit_cost)::numeric >= (0)::numeric))
- `stock_movement_qty_not_zero` — CHECK (((qty_signed)::numeric <> (0)::numeric))
- `stock_movement_ref_valid` — CHECK ((ref_doc_type = ANY (ARRAY['purchase_bill'::text, 'garment'::text, 'stock_transfer'::text, 'invoice'::text, 'count'::text, 'manual'::text])))
- `stock_movement_type_valid` — CHECK ((movement_type = ANY (ARRAY['opening'::text, 'purchase'::text, 'issue'::text, 'return'::text, 'wastage'::text, 'transfer_out'::text, 'transfer_in'::text, 'adjustment'::text, 'sale'::text])))

### `stock_balance`

A materialised balance per item per branch, maintained for speed and fully rebuildable from stock_movement. The movements are the truth; a reconciliation test proves this table agrees with them.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `stock_item_id` | uuid | not null |
| `qty_on_hand` | app.quantity | not null · default `0` |
| `avg_unit_cost` | app.money_amount | not null · default `0` |
| `value_amount` | app.money_amount | not null · default `0` |
| `last_movement_at` | timestamp with time zone |  |
| `recomputed_at` | timestamp with time zone |  |

References: `branch`, `business`, `stock_item`

### `stock_transfer`

Stock moving between outlets. Two halves, so what is in transit is never invisible.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `transfer_no` | text | not null |
| `from_branch_id` | uuid | not null |
| `to_branch_id` | uuid | not null |
| `status` | text | not null · default `'draft'::text` |
| `sent_at` | timestamp with time zone |  |
| `sent_by` | uuid |  |
| `received_at` | timestamp with time zone |  |
| `received_by` | uuid |  |
| `notes` | text |  |

References: `branch`, `business`, `branch`, `app_user`, `app_user`, `branch`

Constraints:

- `stock_transfer_branches_differ` — CHECK ((from_branch_id <> to_branch_id))
- `stock_transfer_received_coherent` — CHECK (((status <> 'received'::text) OR (received_at IS NOT NULL)))
- `stock_transfer_status_valid` — CHECK ((status = ANY (ARRAY['draft'::text, 'in_transit'::text, 'received'::text, 'cancelled'::text])))

### `stock_transfer_line`

What was sent and what actually arrived. A difference requires an explanation.

| column | type | |
|---|---|---|
| `transfer_id` | uuid | not null |
| `stock_item_id` | uuid | not null |
| `qty_sent` | app.quantity | not null |
| `qty_received` | app.quantity |  |
| `variance_reason` | text |  |

References: `business`, `stock_item`, `stock_transfer`

Constraints:

- `stock_transfer_line_qty_positive` — CHECK (((qty_sent)::numeric > (0)::numeric))
- `stock_transfer_line_variance_explained` — CHECK (((qty_received IS NULL) OR ((qty_received)::numeric = (qty_sent)::numeric) OR (variance_reason IS NOT NULL)))

### `customer_material`

Cloth the customer brought. A custody obligation, not an asset: it appears in no valuation, no account and no journal entry. If it is lost, compensation is entered as an expense by a person with a reason - the only path by which it can ever touch the books (BR-05).

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `customer_id` | uuid | not null |
| `order_id` | uuid |  |
| `ownership` | text | not null · default `'customer'::text` |
| `description` | text | not null |
| `colour` | text |  |
| `qty_received` | app.quantity | not null |
| `uom` | text | not null · default `'metre'::text` |
| `condition_notes` | text |  |
| `storage_location` | text |  |
| `received_at` | timestamp with time zone | not null · default `now()` |
| `received_by` | uuid |  |
| `receipt_ack_id` | uuid |  |
| `return_ack_id` | uuid |  |
| `declared_value_non_accounting` | app.money_amount |  |
| `status` | text | not null · default `'held'::text` |

References: `branch`, `business`, `customer`, `sales_order`, `attachment`, `app_user`, `attachment`

Constraints:

- `customer_material_ownership_valid` — CHECK ((ownership = 'customer'::text))
- `customer_material_qty_positive` — CHECK (((qty_received)::numeric > (0)::numeric))
- `customer_material_status_valid` — CHECK ((status = ANY (ARRAY['held'::text, 'partly_issued'::text, 'issued'::text, 'returned'::text, 'written_off'::text])))

### `customer_material_movement`

Received, issued, wasted, returned. Remaining quantity is the sum of these - the figure shown at delivery so leftover cloth is settled, not forgotten.

| column | type | |
|---|---|---|
| `branch_id` | uuid | not null |
| `material_id` | uuid | not null |
| `movement_type` | text | not null |
| `qty_signed` | app.quantity | not null |
| `garment_id` | uuid |  |
| `occurred_at` | timestamp with time zone | not null · default `now()` |
| `performed_by` | uuid |  |
| `reason_code` | text |  |
| `note` | text |  |
| `acknowledgment_id` | uuid |  |

References: `attachment`, `branch`, `business`, `garment`, `customer_material`, `app_user`

Constraints:

- `customer_material_movement_loss_needs_reason` — CHECK (((movement_type <> ALL (ARRAY['wastage'::text, 'written_off'::text])) OR (reason_code IS NOT NULL)))
- `customer_material_movement_qty_not_zero` — CHECK (((qty_signed)::numeric <> (0)::numeric))
- `customer_material_movement_type_valid` — CHECK ((movement_type = ANY (ARRAY['received'::text, 'issued_to_garment'::text, 'wastage'::text, 'returned_to_customer'::text, 'written_off'::text])))

## Not yet assigned to a cluster

`audit_reason_requirement`, `user_credential`
